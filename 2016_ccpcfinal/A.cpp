#include<cstdio>
#include<algorithm>
#include<vector>
using namespace std;
const int maxn = 100000 + 10;
int p[maxn], n;
int cmp(int x, int y)
{
	return x > y;
}

int main(void)
{
	int T;
	scanf("%d", &T);
	for(int ii = 1; ii <= T; ii++)
	{
		
	}
	return 0;
}
